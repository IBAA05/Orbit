import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  /// Brand forest green from the splash screen.
  static const Color primary = Color(0xFF0D6E53);

  /// Pure white used for text, icon tile, and contrast.
  static const Color onPrimary = Color(0xFFFFFFFF);

  /// Slight transparency used for the tagline to soften it against the green.
  static const Color onPrimaryMuted = Color(0xE6FFFFFF); // 90% white
}